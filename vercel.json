{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "env": {
      "PATH": "/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin"
    },
    "buildCommand": "apt-get update && apt-get install -y vlc && npm install"
  }
  